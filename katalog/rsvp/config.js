window.INVITALAB_RSVP_CONFIG = Object.freeze({
    // Isi setelah Worker RSVP selesai di-deploy, tanpa garis miring di akhir.
    apiUrl: "https://YOUR-WORKER.workers.dev",

    // Isi dengan Site Key Cloudflare Turnstile untuk domain undangan.
    turnstileSiteKey: "YOUR_TURNSTILE_SITE_KEY"
});
